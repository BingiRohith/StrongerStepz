import { NextRequest, NextResponse } from 'next/server';
type Point={date:string;price:number};
function buildResult(source:string,title:string,asin:string,points:Point[],note?:string){const prices=points.map(p=>p.price).filter(p=>p>0);const currentPrice=prices.at(-1)!;const low=Math.min(...prices),high=Math.max(...prices),average=Math.round(prices.reduce((a,b)=>a+b,0)/prices.length),sorted=[...prices].sort((a,b)=>a-b),median=sorted[Math.floor(sorted.length/2)];const score=Math.round(Math.max(0,Math.min(100,prices.filter(p=>p>=currentPrice).length/prices.length*100)));return{source,title,asin,currentPrice,low,high,average,median,score,verdict:score>=85?'Exceptional price':score>=70?'Very good price':score>=50?'Fair price':'Expensive price',confidence:points.length>=100?'High':points.length>=30?'Medium':'Low',points,note};}
function demoResult(){const now=new Date();const points:Point[]=Array.from({length:365},(_,i)=>{const d=new Date(now);d.setDate(d.getDate()-(364-i));const seasonal=Math.sin(i/24)*1700,noise=Math.sin(i*.77)*500+Math.sin(i*.17)*350,sale=i%83===0||i%127===0?-2600:0;return{date:d.toISOString().slice(0,10),price:Math.round(29999+seasonal+noise+sale)}});points.at(-1)!.price=24999;return{...buildResult('Demo data','Example product — price intelligence demo','DEMO-ASIN',points,'Illustrative data — not a real product price.'),confidence:'Demo only'};}
function parseAsin(url:string){return url.match(/(?:\/dp\/|\/gp\/product\/|\/dp%2F)([A-Z0-9]{10})/i)?.[1]?.toUpperCase()??null;}
export async function GET(request:NextRequest){if(request.nextUrl.searchParams.get('demo')==='1')return NextResponse.json(demoResult());return NextResponse.json({error:'Send a product URL with POST.'},{status:405});}
export async function POST(request:NextRequest){try{const{url}=await request.json();if(typeof url!=='string'||!/^https?:\/\//i.test(url))return NextResponse.json({error:'Paste a valid product URL.'},{status:400});const host=new URL(url).hostname.toLowerCase();if(!host.includes('amazon.in'))return NextResponse.json({error:'MVP currently supports Amazon India links.'},{status:400});const asin=parseAsin(url);if(!asin)return NextResponse.json({error:'Could not identify the Amazon product ID (ASIN) from this link.'},{status:400});const apiKey=process.env.KEEPA_API_KEY;if(!apiKey)return NextResponse.json({error:'Live price history is not connected yet. Use “Try demo” to test the interface. A price-history provider key is required for accurate historical data.'},{status:503});const response=await fetch(`https://api.keepa.com/product?key=${encodeURIComponent(apiKey)}&domain=10&asin=${encodeURIComponent(asin)}&history=1&stats=365`,{cache:'no-store'});if(!response.ok)return NextResponse.json({error:'The price-history provider could not be reached.'},{status:502});const payload=await response.json(),product=payload.products?.[0];if(!product)return NextResponse.json({error:'No price-history data was found for this product.'},{status:404});const csv:number[]=product.csv?.[0]??[],points:Point[]=[],epoch=new Date('2011-01-01T00:00:00Z').getTime();for(let i=0;i+1<csv.length;i+=2){const minutes=csv[i],value=csv[i+1];if(!Number.isFinite(minutes)||!Number.isFinite(value)||value<0)continue;const date=new Date(epoch+minutes*60000);if(date.getTime()<Date.now()-365*86400000)continue;points.push({date:date.toISOString().slice(0,10),price:Math.round(value/100)});}if(points.length<2)return NextResponse.json({error:'The provider returned insufficient historical price observations.'},{status:422});return NextResponse.json(buildResult('Keepa',product.title??'Amazon product',asin,points,'Based on recorded Amazon price history.'));}catch{return NextResponse.json({error:'Could not process that product link.'},{status:500});}}
